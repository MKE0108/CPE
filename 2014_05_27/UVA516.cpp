#include<iostream>
#include<cmath>
#include<sstream>
#include<string>
#include<vector>
using namespace std;
int main() {
	string s;
	while (getline(cin,s)) {
		stringstream ss(s);
		int temp;
		vector<int> seq;
		while (ss >> temp) {
			seq.push_back(temp);
		}
		unsigned long long x;

	}
}